org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official
# Default backend for the emulator (host loopback). Override with -PBACKEND_BASE_URL=... for a
# real device / deployed backend.
BACKEND_BASE_URL=http://10.0.2.2:8080

# Supabase Auth. The publishable key is public by design (ships in every client; data is
# protected by Row-Level Security), so it is safe to commit and embed.
SUPABASE_URL=https://qvpaeptwdplpflbxpwav.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_iZM8JIEF1H8iHtzTxu2FjA_2XlQeNJl
